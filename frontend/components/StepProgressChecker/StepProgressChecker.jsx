import { LinearProgress } from '@mui/material';

import styles from './styles';

const StepProgressChecker = () => {
  return <LinearProgress {...styles.linearProgressProps} />;
};

export default StepProgressChecker;
